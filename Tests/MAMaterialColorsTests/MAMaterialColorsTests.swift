import XCTest
@testable import MAMaterialColors

final class MAMaterialColorsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MAMaterialColors().text, "Hello, World!")
    }
}
